# Static site — no build step. Netlify publishes the repo root.
[build]
  publish = "."
  command = ""

[[headers]]
  for = "/media/orbit-frames/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
